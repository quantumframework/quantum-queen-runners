---
- hosts: "{{ UM_RUNNERS_GRP|default('runners') }}"
  tasks:
  - name: "Print out a debug message stating that this playbook does nothing"
    debug:
      msg: "This playbook is not implemented."
